// when parsing endpoint ruleset
// while typechecking par RegionName
//   at invalid-rules/wrong-default-type.json5:8
// Invalid type for field "default": Type must match parameter type. Expected `StringType`, found `BooleanType`.
{
  "version": "1.2",
  "parameters": {
    "RegionName": {
      "type": "string",
      "builtIn": "AWS::Region",
      "default": true,
      "required": true
    }
  },
  "rules": [
  ]
}
