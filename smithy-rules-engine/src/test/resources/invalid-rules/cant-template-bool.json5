// while typechecking
//   at invalid-rules/cant-template-bool.json5:21
// while typechecking the endpoint
//   at invalid-rules/cant-template-bool.json5:23
// while checking the URL
// while typechecking "{dyn enabled}.amazonaws.com"
// while typechecking the template `"{dyn enabled}.amazonaws.com"`
//   at invalid-rules/cant-template-bool.json5:24
// while checking {dyn enabled}
// Expected string but found Bool
{
  "version": "1.2",
  "parameters": {
    "enabled": {
      "type": "Boolean",
      "required": true
    }
  },
  "rules": [
    {
      "conditions": [],
      "endpoint": {
        "url": "{enabled}.amazonaws.com"
      },
      "type": "endpoint"
    }
  ]
}
