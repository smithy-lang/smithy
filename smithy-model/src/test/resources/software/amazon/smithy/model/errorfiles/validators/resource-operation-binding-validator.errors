[ERROR] (ResourceOperationBinding) ns.foo#InvalidResourceOperation1 / [0, 0]: This operation is marked with the `instanceOperation` trait but is bound to the `ns.foo#InvalidResource` resource using a collection binding, meaning that one or more of the identifiers of the resource are not bound to members of the operation input using required members.
[ERROR] (ResourceOperationBinding) ns.foo#InvalidResourceOperation2 / [0, 0]: This operation is marked with the `collectionOperation` trait but is bound to the `ns.foo#InvalidResource` resource using an instance binding, meaning that all of the identifiers of the resource are bound to members of the operation input.
