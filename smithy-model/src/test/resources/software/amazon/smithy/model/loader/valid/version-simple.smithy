// Note that when serializing the AST, this library will always
// serialize the full version number of the tool that produced
// the AST. This is requird because multiple IDL models may have
// been loaded all using different, but compatible version numbers.
$version: "1"

