[ERROR] (ReferencesTrait) ns.foo#MyString / [0, 0]: This string shape contains an invalid references trait reference named `another` that targets the `ns.foo#MyResource` resource. References on a string shape can only refer to resource shapes with exactly one entry in its identifiers property, but this shape has the following identifiers: [`abc`, `def`]
