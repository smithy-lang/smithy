{
  "version": "1.0",
  "projections": {
    "valid": {
      "transforms": [
        {"name": "includeTraits", "args": ["sensitive", "required"]}
      ]
    },
    "invalid": {
      "transforms": [
        // This is a comment.
        {"name": "excludeTraits", "args": ["required"]}
      ]
    }
  }
}
