{
    "version": "1.0",
    "plugins": {
        "test-process via run": {
            "command": ["sh", "test-process.sh", "a"],
            "env": {
                "FOO_BAR": "BAZ",
                "FOO_PATH": "${PATH}"
            }
        }
    }
}
